# MongoDB connection string (Bun carga .env automáticamente)
# Copia este archivo a `.env` y ajusta el valor según tu entorno.
MONGO_URI=mongodb://127.0.0.1:27017/horariospp

# Seed de administrador al iniciar
# Si es true, crea automáticamente un usuario admin si no existe
CREATE_ADMIN_ON_START=true

# Credenciales por defecto del administrador
ADMIN_EMAIL=Admin@gmail.com
ADMIN_PASSWORD=Admin
# Tipo de usuario admin (4=admin, 3=profe, 2=profeEspecial, 1=usuario normal)
ADMIN_TYPE=4
